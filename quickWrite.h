#pragma once

// file that adds some name shortening possibilities to avoid writing unnecessarily long declarations and such things, but if used, code must be converted back to work without it!

// shortened from unsigned short
#define ushort unsigned short

// shortened from unsigned int
#define uint unsigned int

// shortened from unsigned long
#define ulong unsigned long

// shortened from wchar_t
#define wchar wchar_t

// shortened from else if
#define elif else if

// shortened from if (debug)
#define ifDebug if (debug)

// shortened from std::to_string
#define toString std::to_string

// shortened from sf::Vector2f
#define vec2f sf::Vector2f

// shortened from sf::Vector2i
#define vec2i sf::Vector2i

// shortened from std::string
#define str std::string

// temp base block size
#define bSize 32